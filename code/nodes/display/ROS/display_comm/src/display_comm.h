#ifndef DISPLAY_COMM_H
#define DISPLAY_COMM_H

#include <ros/ros.h>

#include <display_comm/display_SetContainer.h>
#include <display_comm/display_Write.h>

/*class DisplayComm
{
  public:
    DisplayComm();
    DisplayComm(ros::NodeHandle* np);
    ~DisplayComm();

    // Subscription
    void subscribe(ros::NodeHandle* np);

    // Subscribe to Topics
    
    // Call this before program exits so we don't have double freeing issues
    void shutdown();

    // User functions
    bool Ping();
    
  private:
    // Subscribers
    
    // ROS Service Clients
    
    // ROS services
};*/

#endif //DISPLAY_COMM_H
